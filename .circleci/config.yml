version: 2.1
jobs:
  build:
    machine: true

    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: Builder
          command: |
            - sh .circleci/compile.sh

      - run:
          name: Run unit tests
          command: |
            - sh .circleci/unit_tests.sh

