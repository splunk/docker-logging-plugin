version: 2.1
jobs:
  build:
    machine: true

    working_directory: ~/repo

    steps:
      - checkout
      - run:
          name: Builder
          command: |
            make

      - run:
          name: Run unit tests
          command: |
            .circleci/unit_tests.sh

